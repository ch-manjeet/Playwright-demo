// capture the myntra search bar dropdown & print its elements on searching shirt

/*

import{test,expect, Locator} from '@playwright/test'

test('search for shirt and print the dropdown values',async({page})=>{

    await page.goto('https://www.myntra.com/', { waitUntil: 'commit',timeout: 60000 });

    const searchDropdown:Locator =  page.getByRole('textbox',{name:'Search for products, brands and more'});
    await expect (searchDropdown).toBeVisible();


})

*/
